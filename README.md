# Next-Word-Prediction
Next-Word Predictor: An LSTM-based language model that learns from a text corpus to predict and generate coherent text. Includes preprocessing, tokenization, sequence creation, one-hot encoding, model checkpoints, and temperature-based next-word generation for dynamic text output.
